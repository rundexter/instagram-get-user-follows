/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [
        {
            "id": "userId",
            "title":"User ID [default self]"
        }
    ],
    //Required. Outputs the module emits.
    "outputs": [
        {
            "id": "users",
            "title":"Array of users"
        }
    ],
    "title": "Instagram Get User Follows" /* Required.A display name for the module */
}
